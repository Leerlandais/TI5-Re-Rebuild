{% extends 'template.html.twig' %}

{% block title %}Article{% endblock %}

{% block banner %}
    <br><br>

<div class="text-center my-5">
    <h3>Article Index</h3>
    <ul class="list-inline widget-list-inline">

        <li class="list-inline-item"><a href="{{ path('admin_article_index') }}">back to list</a></li>
        <li class="list-inline-item"><a href="{{ path('admin_article_edit', {'id': article.id}) }}">edit</a></li>
    </ul>
</div>
    <table class="table text-center mb-5">
        <tbody>

            <tr>
                <th>Title</th>
                <td>{{ article.title }}</td>
            </tr>

            <tr>
                <th>Text</th>
                <td>{{ article.text }}</td>
            </tr>
            <tr>
                <th></th>
                <td>
                    {{ include('admin_article/_publish_form.html.twig') }}
                </td>
            </tr>
        </tbody>
    </table>


 {#    {{ include('admin_article/_delete_form.html.twig') }}   #}
{% endblock %}
